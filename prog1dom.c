#include <stdio.h>

int main()

{

	const float napis[] = "Pi = 3.1415926535\n";
	const char Przedmiot[] = "Programowanie\n";
	const unsigned int Rok=-1;
	long a=0;
	printf("Int posiada max %d bitow\n", sizeof (int));
	printf("Char posiada max %d bitow\n", sizeof (char));
	printf("Short posiada max %d bitow\n", sizeof (short));
	printf("Long posiada max %d bitow\n", sizeof (long)); 
	
return 0;
}
